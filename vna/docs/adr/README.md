# ADR

本目录用于记录 Architecture Decision Record（架构决策记录）。

## 使用方式

1. 复制 `ADR_TEMPLATE.md` 并按序号创建，例如：`ADR-0001-xxx.md`。
2. 每个 ADR 只讨论一个关键决策。
3. 决策状态建议：Proposed / Accepted / Deprecated / Superseded。

## 何时写 ADR

- 引入新的跨模块架构约束。
- 替换核心技术路径（例如构建链、通信协议、驱动抽象）。
- 存在多个可选方案且需要长期追溯。
